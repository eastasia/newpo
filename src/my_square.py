def square(x):
      """ Thsi is asample python
      """ I wan to return this 
      """ this $a uses the ** is additional
      """
      return (x ** 2)

def my_square2(x):
     """ uses the * operator to calculate square 
     """
     return (x * )

print square(102)
print( my_square2(2)))
